# 学习星球 UI 规范（V1.0）

## 1. 设计定位

学习星球是一款面向三年级学生与家长共同使用的平板学习任务管理系统。界面采用**扁平化、低学习成本、卡通可爱**的视觉语言：学生端侧重鼓励与探索，家长端侧重效率与清晰，两端保持相同的颜色、图标和状态语义。

### 1.1 体验原则

- **一眼看懂**：每个页面只突出一个主要目标；信息以图标、数字、短句和颜色表达。
- **一触完成**：学生的关键操作不超过 3 步；主按钮固定在易触达位置。
- **温和鼓励**：对未完成、逾期、补充等状态使用中性、可行动的提示，不制造压力。
- **大而稳定**：为平板触控设计，不依赖悬停；点击区域、卡片和表单控件保持足够大。
- **可爱但不幼稚**：使用圆润几何、手绘感图标和明快色块，不使用复杂纹理、拟物阴影或大面积渐变。

### 1.2 端侧气质

| 端侧 | 视觉重点 | 信息密度 | 核心感受 |
| --- | --- | --- | --- |
| 学生端 | 成长星球、任务卡、奖励反馈 | 低 | 明确、有趣、被鼓励 |
| 家长端 | 概览、筛选、审核、统计 | 中 | 清楚、可靠、省时间 |
| 管理端 | 表格、筛选、配置 | 中高 | 克制、易核对 |

## 2. 布局与响应式

### 2.1 设计基准

- 基准画板：`1280 x 800 px`（10-12 英寸平板横屏）。
- 最小支持：`768 px` 宽；小于该宽度时切换为单栏移动布局。
- 横屏优先展示侧边栏与内容区；竖屏使用顶部导航或底部导航，内容保持单栏。
- 页面内容最大宽度为 `1440 px`，在更大屏幕居中显示；左右安全边距为 `24 px`，窄屏为 `16 px`。

### 2.2 页面骨架

| 区域 | 横屏 | 竖屏 |
| --- | --- | --- |
| 顶部栏 | 高 `64 px`，品牌、当前身份、消息、头像 | 高 `60 px`，保留返回、标题和必要操作 |
| 学生端导航 | 左侧 `96 px` 图标栏，图标下有短标签 | 底部 `64 px`，最多 4 个一级入口 |
| 家长端导航 | 左侧 `216 px`，图标加文字 | 可折叠抽屉或底部 4 项主导航 |
| 内容区 | 24 px 内边距，12 列网格 | 16 px 内边距，4 列网格 |
| 主操作 | 详情页底部固定操作区 | 底部固定，避开系统手势区 |

### 2.3 网格与间距

采用 `4 px` 基础单位，仅使用 `4 / 8 / 12 / 16 / 24 / 32 / 40 / 48 px` 间距。常规卡片间距为 `16 px`，页面区块间距为 `24 px`。学生端任务卡单列展示，家长端概览卡可使用 2-4 列栅格。

## 3. 视觉基础

### 3.1 色彩

主色为天空蓝，辅助色为成长绿。色彩用于表达类别、状态和奖励，不将颜色作为唯一信息来源。

| 用途 | 色值 | 使用规则 |
| --- | --- | --- |
| 品牌蓝 / Primary | `#2F80ED` | 主按钮、选中导航、进度、可交互重点 |
| 深蓝 / Primary pressed | `#1769C2` | 按压态、重点文字 |
| 浅蓝 / Primary soft | `#EAF4FF` | 选中背景、信息容器 |
| 成长绿 / Success | `#36B37E` | 已完成、通过、正向提示 |
| 浅绿 / Success soft | `#E9F8F1` | 完成状态底色 |
| 活力黄 / Reward | `#F9B940` | 星星、奖励、轻提醒 |
| 浅黄 / Reward soft | `#FFF6DB` | 奖励卡背景 |
| 珊瑚橙 / Warning | `#F2994A` | 需补充、临近截止、温和提醒 |
| 柔红 / Danger | `#EB5757` | 未通过、危险操作，仅限明确错误 |
| 紫罗兰 / Accent | `#8B6FE8` | 英语、成长等级等辅助分类 |
| 墨蓝灰 / Text primary | `#243B53` | 标题与正文 |
| 中性灰 / Text secondary | `#627D98` | 说明、元信息 |
| 浅灰蓝 / Border | `#D9E2EC` | 分割线、输入框边框 |
| 页面底色 | `#F7FAFC` | 大面积背景 |
| 纯白 | `#FFFFFF` | 卡片、弹窗、输入区域 |

分类可采用固定色组：语文 `#F2994A`、数学 `#2F80ED`、英语 `#8B6FE8`、阅读 `#36B37E`、运动 `#EB5757`、生活 `#F9B940`。同一分类在所有页面保持颜色和图标一致。

### 3.2 字体与文字

- 中文字体：优先使用系统无衬线字体（PingFang SC / Microsoft YaHei）；数字使用同字体的等宽数字特性。
- 字号不随视口缩放；正文不小于 `14 px`，学生端核心文字不小于 `16 px`。
- 字重：标题 `600`，按钮与标签 `600`，正文 `400`。
- 行高：标题 `1.25`，正文 `1.5`，说明 `1.4`；字间距始终为 `0`。

| 样式 | 尺寸 / 行高 | 使用场景 |
| --- | --- | --- |
| 页面标题 | `28 / 36 px` | 首页、一级页面 |
| 区块标题 | `20 / 28 px` | 卡片组、模块标题 |
| 卡片标题 | `18 / 26 px` | 任务、学生、审核项 |
| 正文 | `16 / 24 px` | 学生端描述、主要内容 |
| 辅助正文 | `14 / 20 px` | 时间、说明、统计标签 |
| 数字重点 | `32 / 40 px` | 完成数、星星数、进度 |

### 3.3 圆角、边框与阴影

- 页面区块不使用悬浮大卡片；仅任务、学生、统计项、弹窗等独立对象使用卡片。
- 卡片圆角 `8 px`；按钮、输入框圆角 `8 px`；圆形头像和奖励徽章使用 `50%`。
- 默认边框为 `1 px #D9E2EC`。卡片只使用极轻阴影：`0 2px 6px rgba(36, 59, 83, 0.08)`；避免厚重阴影和玻璃效果。
- 图标可有 `1.5-2 px` 深色描边与简化高光，形成卡通感；界面容器保持扁平。

## 4. 图标与插画

### 4.1 图标规范

- 图标风格为圆润、简化、可辨识的卡通线面结合风格，线条粗细统一为 `2 px`。
- 默认采用品牌蓝或分类色；禁用时使用 `#9FB3C8`；不可用灰色替代状态图标。
- 导航图标 `28 px`，任务分类图标 `40 px`，卡片操作图标 `24 px`，空状态插画 `120-160 px`。
- 每个图标必须搭配文字标签或无障碍名称，学生端不能只凭抽象图标理解功能。
- 不混用写实、线框、3D、表情包等多套图标风格；全系统使用同一套图标库与自定义分类插画。

### 4.2 推荐图形语义

| 场景 | 图形建议 |
| --- | --- |
| 语文小屋 | 书本、小屋、铅笔 |
| 数学乐园 | 数字方块、计算器、尺子 |
| 英语角 | 对话气泡、字母积木 |
| 阅读时光 | 打开的书、台灯 |
| 运动能量站 | 球、运动鞋、闪电 |
| 生活小能手 | 背包、扫帚、植物 |
| 已完成 | 绿色圆形勾选 |
| 待审核 | 蓝色小沙漏或放大镜 |
| 需补充 | 橙色对话气泡加号 |
| 逾期 | 橙色时钟，不使用哭脸或警告三角 |
| 奖励 | 五角星、月亮、太阳，表情保持积极克制 |

## 5. 组件规范

### 5.1 触控与交互状态

- 所有可点击区域最小为 `44 x 44 px`；学生端主操作最小高度 `52 px`。
- 支持点击、滑动、长按（仅用于照片排序等非关键功能），不依赖双击、右键或悬停。
- 点击态：背景加深或描边增强，同时缩放至 `0.98`；时长 `120 ms`。
- 焦点态：`2 px` 品牌蓝外描边；禁用态保持文字可读，对比度不低于 `3:1`。
- 任务完成、审核通过、发放奖励均需立即显示结果，不允许只在后台悄悄更新。

### 5.2 按钮

| 类型 | 外观 | 使用规则 |
| --- | --- | --- |
| 主按钮 | 蓝底白字，可配单个图标 | 每个视图最多 1 个，例如“提交完成” |
| 成功按钮 | 绿底白字 | 仅用于“我完成啦”“通过并奖励”等完成动作 |
| 次按钮 | 白底蓝边蓝字 | 返回、保存草稿、查看详情 |
| 文字按钮 | 无边框蓝字 | 次要操作，不放置危险动作 |
| 图标按钮 | `44 x 44 px`，带悬浮提示 | 返回、筛选、编辑、更多操作 |
| 危险按钮 | 红色描边或红底 | 删除、解绑等不可逆操作，必须二次确认 |

按钮文案使用动作短句：`开始任务`、`拍张照片`、`提交完成`、`通过并奖励`。避免使用“确定”“提交”等脱离语境的泛化词。

### 5.3 任务卡

学生端任务卡高度自适应，最小 `124 px`，包含左侧分类图标、中部标题与元信息、右侧状态/星星。整张卡可打开详情；底部仅保留一个最相关的主操作。标题最多两行，说明最多一行，超出截断。

任务状态使用“图标 + 文字 + 色彩”的组合：

| 状态 | 标签 | 样式 |
| --- | --- | --- |
| 未开始 | 等你开始 | 蓝色描边标签 |
| 进行中 | 已保存草稿 | 蓝色浅底标签 |
| 待审核 | 等爸爸妈妈看看 | 蓝色浅底 + 沙漏 |
| 已完成 | 完成啦 | 绿色浅底 + 勾选 |
| 需补充 | 再补充一点 | 橙色浅底 + 对话图标 |
| 未通过 | 这次先不算完成 | 红色浅底 + 说明入口 |
| 逾期 | 这件事还在等你 | 橙色浅底 + 时钟 |

### 5.4 表单与选择器

- 输入框高度 `48 px`，标签始终置于输入框上方，必填项以“必填”文字表示，不只用星号。
- 学生端优先采用单选卡、图标网格、步进器和日期条；减少下拉菜单与长文本输入。
- 家长端筛选以横向筛选条或抽屉呈现，已选条件显示为可关闭标签。
- 日期选择默认显示周视图。日期单元最小 `48 x 60 px`，同时显示星期与日期；今天使用蓝色描边，选中日期使用蓝色实底。
- 破坏性提交前使用清晰确认弹窗，说明影响，并提供“取消”和明确动作，例如“确认解绑”。

### 5.5 反馈上传

- 反馈入口用四个大图标按钮：`拍照`、`相册`、`录视频`、`写一句话`；根据任务要求隐藏不适用入口。
- 图片缩略图为 `96 x 96 px`，视频显示首帧、播放图标和时长；上传进度必须可见。
- 上传失败显示原因和“重新上传”；草稿自动保留，不因返回页面丢失。
- 提交前显示反馈清单与任务标题，主按钮为“确认提交”。

### 5.6 反馈、空状态与提示

- 成功提示使用绿色勾选和一句结果文字，例如“已经交给爸爸妈妈看啦”。
- 空状态使用相关小插画、说明和一个行动入口，例如“今天的任务都完成了”。
- 错误提示说清问题与解决方式，例如“视频超过 100 MB，请换一个更短的视频”；不显示技术错误码。
- Toast 停留 3 秒，可手动关闭；审核、删除、奖励等关键结果使用页面内结果或弹窗，不只使用 Toast。

## 6. 核心页面规范

### 6.1 学生端：今日任务

页面顶部依次为问候语与头像、周日期条、当日任务数量和进度条。任务列表按“未完成、需补充、待审核、已完成”排序，逾期任务单独置顶但使用温和文案。完成率以“2 / 4 件完成”配合进度条表示，不单独用百分比要求学生理解。

任务详情优先展示“要做什么”“怎么提交”“能获得几颗星星”三项；参考素材紧随任务说明。底部固定主操作随状态变化：`开始任务`、`补充反馈`、`确认提交` 或 `已提交，等待查看`。

### 6.2 学生端：成长星球

以小星球为主视觉，星星、月亮、太阳围绕展示，但数值和换算必须清晰可读。主视觉下固定展示“距离下一个奖励”的进度条和短句。最近奖励采用时间线列表，显示任务名称、奖励数量和家长鼓励语。动画只用于获得奖励和等级升级，结束后停在可读的静态结果上。

### 6.3 家长端：首页与学生管理

家长首页顶部提供学生切换，主体为今日概览：待审核、今日完成、逾期提醒、累计星星。指标卡可点击进入对应列表。学生管理以头像卡片呈现，每张卡显示进度、待审核、逾期和等级；创建学生使用分步表单，单步只处理一组信息。

### 6.4 家长端：任务分配与审核

任务创建采用分组表单：任务内容、分配对象与日期、反馈与奖励。频繁选项（分类、奖励星星、是否审核）使用大尺寸单选/步进控件；保存模板为次级操作。审核详情采用左侧反馈预览、右侧任务信息与审核操作的双栏布局；竖屏时按“反馈 - 任务信息 - 审核操作”顺序单列排列。

审核动作保持互斥且层级明确：主操作为绿色“通过并奖励”，次操作为“需要补充”，危险操作“本次不计完成”置于更多操作或明确分区，避免误触。

### 6.5 数据统计与管理端

统计使用柱状图、环形进度和趋势线等易读图表，不使用信息密集的仪表盘。每张图必须提供具体数值、时间范围、学生范围与空状态。管理端沿用同一色彩语义，但减少装饰插画和奖励动画，优先保证表格、筛选、批量操作与审计记录的可读性。

## 7. 动效与声音

- 常规过渡 `160-220 ms`，使用缓入缓出；页面切换不使用大幅缩放或翻页效果。
- 星星奖励可采用 400-600 ms 的轻量飞入动画，单次最多展示 5 颗星；支持系统“减少动态效果”设置。
- 不自动播放背景音乐；拍照、上传、成功等声音反馈默认关闭或跟随系统静音状态。
- 加载超过 300 ms 时显示骨架屏；超过 5 秒给出文字进度或可重试操作。

## 8. 无障碍与质量基线

- 常规正文与背景对比度至少 `4.5:1`；大号文字、图标和控件至少 `3:1`。
- 所有状态均有文字和图标，不只依赖红绿颜色区分。
- 支持系统文字放大至 130%，按钮和卡片内容自动换行，不裁切、不重叠。
- 关键图标、头像、媒体缩略图提供可读名称；装饰插画不参与读屏。
- 表单错误在字段附近就地展示，并把焦点移至第一个错误字段。
- 横竖屏切换时保留当前日期、筛选条件、编辑草稿与滚动上下文。

## 9. 交付检查清单

- 设计稿包含横屏、竖屏、加载、空、失败、禁用、选中、完成与逾期状态。
- 所有学生端高频操作均可在 3 步内完成，且主按钮触控高度不少于 `52 px`。
- 每个任务状态在任务卡、详情页、家长审核列表和统计中使用相同名称、颜色和图标。
- 图标、插画、头像授权可用于商业产品；不混用不同笔触与透视风格。
- 在 `1280 x 800`、`1024 x 768`、`768 x 1024` 三个平板视口下检查文字换行、固定操作区和媒体上传布局。
