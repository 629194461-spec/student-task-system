# 萌宠一期成长闭环技术设计

## 1. 一期边界

一期实现“家长开启 - 学生领养 - 完成学习 - 获得 EXP - 宠物升级 - 轻互动 - 查看成长记录”的完整闭环。它不改变既有任务、阅读、审核和星星奖励的业务结果，只在最终完成事件发生后独立结算萌宠数据。

| 一期交付 | 说明 |
| --- | --- |
| 家长开启与配置 | 对绑定学生启用/暂停，设置每日互动时长、声音和减少动态效果 |
| 领养与主页 | 三个已确认基础品种、昵称、出战宠物、星星小屋、等级/EXP/今日状态 |
| 学习结算 | 普通任务、每日任务完成度、阅读打卡三类 EXP；任务完成时发基础食物 |
| 轻互动 | 摸摸、喂食、清洁、玩耍；每日首次获得 EXP，服务端幂等 |
| 成长记录 | 学生简化版与家长审计版 EXP 流水 |
| 等级与解锁 | LV1-LV100、升级汇总、阶段成长外观、基础动作解锁记录 |

一期不做题库与每日答题、装扮间、场景切换、伙伴册/重新领养、管理员资源后台。这些数据结构会预留，但不暴露未完成入口。所有学生默认只有一只出战宠物，领养后不可在一期内切换。

## 2. 与现有系统的接入点

当前任务和阅读的完成状态在 [server.mjs](/Users/juniorhu/Documents/ChatGPT/小学生学习任务管理系统/server.mjs) 中直接落库：

| 既有节点 | 当前结果 | 一期新增动作 |
| --- | --- | --- |
| `POST /api/student/tasks/:id/submit` | 无审核任务直接完成；有审核任务变为待审核 | 写入提交时出战宠物与归属日期；直接完成时结算 |
| `POST /api/parent/tasks/:id/review` | 审核通过后任务完成并发放星星 | 在同一事务内结算任务 EXP 与每日完成度 |
| `POST /api/student/reading/checkins` | 无审核打卡直接完成；有审核任务待审核 | 写入提交时出战宠物与归属日期；直接完成时结算 |
| `POST /api/parent/reading/checkins/:id/review` | 审核通过后更新阅读进度并发放星星 | 在同一事务内结算阅读 EXP |

萌宠结算失败不能回滚任务状态、阅读状态或星星奖励。单体服务一期采用 `pet_pending_events` 待重试表，而不是引入消息队列：原业务先正常提交，萌宠事件在独立事务中处理；失败时记录事件并由每次服务启动和管理员诊断入口重试。

## 3. 数据模型与迁移

### 3.1 新表

| 表 | 核心字段与约束 | 用途 |
| --- | --- | --- |
| `pet_species` | `code UNIQUE`、名称、性格短句、封面资源、状态、排序、版本 | 预置星环兔、云朵猫、暖阳小熊猫 |
| `student_pet_settings` | `student_id UNIQUE`、`enabled`、`enabled_at`、每日互动时长、声音、减少动态效果、更新人 | 家长控制与历史不追溯边界 |
| `student_pets` | 学生、品种、昵称、`level`、`total_exp`、`active UNIQUE(student_id)`、领养时间 | 当前出战宠物和读取缓存 |
| `pet_daily_states` | `UNIQUE(student_pet_id, business_date)`、心情、饱足、整洁、当日 EXP、互动秒数 | 今日状态和每日总上限 |
| `pet_exp_ledger` | `UNIQUE(student_pet_id, source_type, source_id, rule_version)`、归属日期、增减值、规则快照、余额、原因 | 审计事实来源和幂等键 |
| `pet_interactions` | `UNIQUE(student_pet_id, business_date, type)`、`request_id UNIQUE`、EXP、库存变动 | 防重复互动与防重复扣食物 |
| `pet_inventory` | `UNIQUE(student_pet_id, item_code)`、数量、更新时间 | 一期只使用 `basic_food` |
| `pet_unlocks` | `UNIQUE(student_pet_id, content_type, content_code)`、条件快照、解锁时间 | 动作与阶段内容永久解锁 |
| `pet_pending_events` | `event_id UNIQUE`、来源、宠物、归属日期、载荷、状态、重试数、错误信息 | 结算失败后的补偿队列 |

### 3.2 既有表扩展

| 表 | 新字段 | 写入时机 |
| --- | --- | --- |
| `task_templates` | `pet_exp_weight INTEGER NOT NULL DEFAULT 1 CHECK(… IN 1,2,3)` | 创建/编辑模板时设置，默认 1 |
| `tasks` | `pet_exp_weight_snapshot`、`submitted_active_pet_id`、`pet_business_date` | 分配时复制权重；学生首次提交时冻结宠物与归属日期 |
| `reading_checkins` | `submitted_active_pet_id`、`pet_business_date` | 学生首次提交时冻结宠物与归属日期 |

迁移坚持已有项目的 `CREATE TABLE IF NOT EXISTS` 与 `PRAGMA table_info` 补列方式。新增字段都允许为空，使旧任务和历史打卡永远不会被追溯结算。`pet_species` 使用版本化资源路径，初始状态仅写入三条上架品种记录。

## 4. 后端领域服务

在 `server.mjs` 中新增独立的萌宠领域函数，路由只负责鉴权、参数校验和事务边界。

| 服务 | 核心职责 |
| --- | --- |
| `getActivePet(studentId)` | 查询已启用、已领养且出战的宠物 |
| `capturePetSubmission(taskOrCheckin)` | 在学生提交时冻结 `submitted_active_pet_id` 和 `pet_business_date`；不覆盖已有快照 |
| `settlePetEvent(event)` | 校验模块开启时间和宠物有效性，写入幂等流水、应用来源/总上限、更新缓存等级与解锁 |
| `settleTaskCompletion(task)` | 按 `10 × 权重` 结算任务 EXP，每日任务来源最多 50 EXP，并发放最多 2 份基础食物 |
| `reconcileDailyCompletion(studentId, date)` | 根据原归属日期的有效任务和权重，补齐 50%/80%/100% 的 5/10/15 EXP 档位 |
| `settleReadingCompletion(checkin)` | 每日最多一条有效完成打卡获得 5 EXP |
| `applyInteraction(pet, type, requestId)` | 初始化当日状态、校验互动冷却/库存、仅首次有效互动发放 EXP |
| `rebuildPetBalance(petId)` | 管理员异常处理和将来抵扣流水时，按流水重算 `total_exp`、等级和当日缓存 |

### 4.1 结算事务

一次成功结算在同一个 SQLite 事务内完成：

```text
校验宠物与模块状态
  -> INSERT OR IGNORE 幂等流水
  -> 计算来源日上限及每日 80 EXP 总上限
  -> 更新 pet_daily_states.earned_exp
  -> 更新 student_pets.total_exp / level
  -> 写入 food、动作和阶段解锁
  -> COMMIT
```

当流水键已存在时直接读取既有结果并返回，不重复增加 EXP 或库存。因上限造成的未获经验也保留一条 `delta = 0` 流水和规则快照，便于家长查看“本次成长能量已达上限”的原因。

### 4.2 规则快照

一期固定 `rule_version = pet-exp-v1`。每条流水的 JSON 快照至少保存：理论 EXP、最终 EXP、来源上限、全局上限、权重、归属日期和事件时间。之后修改规则只新增版本，绝不覆盖历史流水。

## 5. 一期经验与等级规则

| 来源 | EXP | 来源日上限 | 幂等来源键 |
| --- | ---: | ---: | --- |
| 普通任务完成 | `10 × pet_exp_weight_snapshot` | 50 | `task_complete:task:{taskId}` |
| 每日任务完成度 | 50%/80%/100% 累计 5/10/15 | 15 | `daily_completion:{date}:50/80/100` |
| 阅读打卡完成 | 5 | 5 | `reading_complete:checkin:{checkinId}` |
| 摸摸 | 1 | 互动总计 5 | `interaction:{date}:pet` |
| 喂食 | 2 | 互动总计 5 | `interaction:{date}:feed` |
| 清洁 | 1 | 互动总计 5 | `interaction:{date}:clean` |
| 玩耍 | 1 | 互动总计 5 | `interaction:{date}:play` |

- 每个归属日期所有来源合计最多 80 EXP。阅读同日多条完成记录只让第一条获得 5 EXP，其余记录写零 EXP 审计流水。
- 提交需审核的任务或打卡，`pet_business_date` 在学生提交时冻结；家长晚审核仍计入原提交日期。
- 模块暂停、未领养、领养前提交或宠物不再有效时，不产生萌宠流水；历史不补发。
- 任务权重修改只允许影响未提交任务；提交后使用快照。
- 升级公式与需求文档一致：`100 + 20 × floor((level - 1) / 10)`；单次多级提升只在前端展示一次汇总反馈。

## 6. API 设计

### 6.1 学生端

| 方法 | 路径 | 返回/行为 |
| --- | --- | --- |
| `GET` | `/api/student/pet` | 模块状态、当前宠物、EXP、等级、今日状态、食物数、最近流水、基础解锁 |
| `GET` | `/api/student/pets/adoption` | 三个可领养基础品种及已选状态 |
| `POST` | `/api/student/pets/adopt` | `{ speciesCode, nickname }`；校验开启、绑定家长、敏感词与昵称 1-12 字符 |
| `POST` | `/api/student/pet/interactions` | `{ type, requestId }`；返回最终状态、库存和本次 EXP |
| `GET` | `/api/student/pet/growth?cursor=` | 儿童可读的成长流水分页 |

### 6.2 家长与管理员

| 方法 | 路径 | 权限 |
| --- | --- | --- |
| `GET` | `/api/parent/students/:studentId/pet` | 绑定家长或管理员 |
| `PUT` | `/api/parent/students/:studentId/pet-settings` | 绑定家长或管理员；开启/暂停及配置 |
| `GET` | `/api/parent/students/:studentId/pet-ledger` | 绑定家长或管理员；按日期和来源筛选 |
| `GET` | `/api/admin/pet-species` | 管理员；一期只读，用于确认预置数据 |

所有写接口沿用既有 session 鉴权；学生只能访问自己的宠物，家长仍通过现有 `canManageStudent` / 绑定关系校验。

## 7. 前端页面与状态

### 7.1 学生端

新增底部导航“我的萌宠”，沿用现有 `app.js` 的单页渲染与 `loadPage` 缓存模式。

| 页面状态 | 页面内容 | 主操作 |
| --- | --- | --- |
| 未开启 | 简洁提示“请家长在学生详情中开启” | 无 |
| 待领养 | 三张品种卡、性格短句、待机概念形象、昵称输入与二次确认 | 领养 |
| 已启用 | 星星小屋、宠物、LV/EXP/今日能量、心情/饱足/整洁、四个互动入口 | 一次最多两步完成互动 |
| 已暂停 | 静态宠物、等级与成长记录；互动均禁用 | 查看历史 |
| 成长记录 | “完成语文任务，获得 10 EXP”等儿童可理解流水 | 查看更多 |

首版只加载当前角色的静态概念占位和 CSS 微动画。正式上线前必须替换为已拆分的 WebP/AVIF 动作资源；首屏关键资源总量不超过 8MB，所有按钮至少 44×44px。

### 7.2 家长端

学生详情增加“萌宠”页签或概要卡：模块开关、当前宠物、等级、今日 EXP、互动时长、声音和减少动态效果。成长流水页面支持按日期/来源查看，并可跳转回任务或阅读原记录。暂停只关闭新互动与新结算，不影响查看历史。

## 8. 验收场景

| 场景 | 预期 |
| --- | --- |
| 家长未开启模块 | 学生不出现可用领养/互动入口，完成任务不写萌宠流水 |
| 开启后首次领养 | 仅可领养一次，创建 LV1/0 EXP、默认状态、星星小屋和基础解锁 |
| 无审核任务完成 | 星星奖励与萌宠 EXP 均只各入账一次；刷新和重试不重复增加 |
| 审核任务晚一天通过 | EXP 仍计入学生原提交日，且使用提交时出战宠物 |
| 同日任务超过 50 EXP | 任务来源最多入账 50，剩余记录保留为零 EXP 审计流水 |
| 同日阅读多次完成 | 最多一条记录入账 5 EXP |
| 四类互动重复点击 | 状态可反馈，首次各自获得 EXP，库存不重复扣除 |
| 家长暂停后 | 可查看宠物与流水；互动、后续任务/阅读的萌宠结算均停止 |
| 任务审核重试或多家长并发审核 | 任务原状态和星星奖励遵循既有防重复逻辑；萌宠流水唯一键保证不重复 |
| 资源加载失败 | 学生仍可读取等级、EXP、成长记录并正常返回 |

## 9. 实施顺序

1. 编写 SQLite 迁移、三只品种种子数据和萌宠领域单元测试。
2. 实现领养、家长开关、主页读取、EXP 流水与等级服务。
3. 将任务与阅读四个完成节点改为调用事件结算服务，并完成幂等、上限、审核延迟场景测试。
4. 实现学生领养页、萌宠主页、互动与成长记录；实现家长设置与流水。
5. 使用星环兔的分层原型完成桌面、iPad、手机视觉与性能验证，再接入云朵猫和暖阳小熊猫。
6. 通过全量回归后再进入二期：每日答题、装扮、场景、伙伴册与管理员资源管理。
