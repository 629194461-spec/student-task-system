services:
  learning-planet:
    build: .
    ports:
      - "4173:4173"
    env_file:
      - .env
    volumes:
      - learning-planet-data:/app/data
    restart: unless-stopped

volumes:
  learning-planet-data:
