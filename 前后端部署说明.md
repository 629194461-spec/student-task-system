# 学习星球前后端部署说明

文档版本：1.2
适用系统版本：1.0.0
更新时间：2026-09-10

## 1. 系统部署形态

本系统采用前后端同源部署：

- 前端为 `index.html`、`app.js`、`styles.css` 和 `assets/` 静态资源。
- 后端为 `server.mjs`，使用 Node.js 原生 HTTP 服务提供静态资源和 `/api/*` 接口。
- 数据库为 Node.js 24 内置 SQLite，文件位于应用工作目录下的 `data/learning-planet.db`。
- 用户、家长学生关系、任务、任务模板、分类、奖励、会话和上传文件 URL 存储在 SQLite 中。
- 用户上传的头像、任务资料和学生反馈存储在腾讯云 COS，数据库只保存访问 URL。系统 Logo、成长形象和任务分类图标属于随版本发布的静态资源，保留在 `assets/`。

生产环境推荐采用以下链路：

```text
用户浏览器 -> HTTPS/Nginx -> Node.js 单实例 -> SQLite/WAL
                                      -> 腾讯云 COS（上传文件）
```

前端不可直接通过 `file://` 打开。必须访问 Node.js 服务或反向代理提供的 HTTP/HTTPS 地址，否则登录和业务接口无法工作。

## 2. 部署前提

### 2.1 服务器建议

小规模家庭或班级使用建议：

- Linux x86_64 或 arm64 服务器
- 2 核 CPU
- 2 GB 内存
- 10 GB 可用磁盘，并根据上传文件数量扩容
- Docker Engine 24+ 与 Docker Compose v2，或 Node.js 24+
- 可用域名和有效 HTTPS 证书

### 2.2 端口

- 应用容器默认监听 `4173`。
- 公网只开放反向代理的 `80/443`。
- 不建议将 `4173` 直接暴露到公网。

### 2.3 初始化账号

首次启动只会创建超级管理员，不会创建演示学生、家长或任务：

| 用户名 | 初始密码 | 角色 |
| --- | --- | --- |
| `admin` | `admin@2026` | 超级管理员 |

正式上线前应限制管理入口的访问范围，并通过系统的密码修改接口变更默认密码。管理员密码应至少 10 位并同时包含字母和数字。默认凭据不得写入公开部署日志、群聊或运维截图。

### 2.4 角色与数据边界

| 功能 | 超级管理员 `admin` | 普通家长 | 学生 |
| --- | --- | --- | --- |
| 家长账号管理 | 全部可管理 | 不可见 | 不可见 |
| 学生管理 | 查看全部，可重新关联家长 | 仅管理关联学生 | 仅查看本人数据 |
| 任务分类 | 可新增、修改、删除 | 菜单不可见，仅在任务表单中使用 | 不可管理 |
| 任务模板 | 可查看可用模板，仅修改本人创建模板 | 查看开放模板及本人模板，仅修改本人模板 | 不可见 |
| 任务、审核、统计 | 全部学生 | 仅关联学生 | 仅本人任务与成长数据 |

权限由后端接口强制校验，前端菜单隐藏不是唯一权限措施。

## 3. 环境变量

复制环境变量模板：

```bash
cp .env.example .env
```

默认内容如下：

```dotenv
NODE_ENV=production
PORT=4173
HOST=0.0.0.0
APP_TIMEZONE=Asia/Shanghai
STORAGE_DRIVER=cos
COS_BUCKET=examplebucket-1250000000
COS_REGION=ap-guangzhou
COS_SECRET_ID=替换为子账号SecretId
COS_SECRET_KEY=替换为子账号SecretKey
COS_PUBLIC_BASE_URL=
COS_PATH_PREFIX=learning-planet
```

变量说明：

| 变量 | 必填 | 建议值 | 说明 |
| --- | --- | --- | --- |
| `NODE_ENV` | 是 | `production` | 开启生产错误隐藏和 Secure Cookie |
| `PORT` | 是 | `4173` | Node.js 服务监听端口 |
| `HOST` | 是 | `0.0.0.0` | 容器内必须监听全部网卡 |
| `APP_TIMEZONE` | 否 | `Asia/Shanghai` | 业务日期、周榜和月榜使用的时区 |
| `DATA_DIR` | 否 | 工作目录下的 `data/` | SQLite 数据目录。仅本地或隔离测试时设置为独立的绝对路径；生产 Docker 环境不要设置，继续使用挂载的 `/app/data` 卷。 |
| `STORAGE_DRIVER` | 是 | `cos` | 生产环境使用 `cos`；开发和测试可使用 `local`。 |
| `COS_BUCKET` | COS 必填 | `bucketname-APPID` | 存储桶完整名称，必须包含 APPID。 |
| `COS_REGION` | COS 必填 | `ap-guangzhou` | 存储桶所属地域。 |
| `COS_SECRET_ID` | COS 必填 | 子账号密钥 | 只授予目标路径所需的上传、删除权限。 |
| `COS_SECRET_KEY` | COS 必填 | 子账号密钥 | 仅保存在服务端 `.env`，不得发送给浏览器或提交 Git。 |
| `COS_PUBLIC_BASE_URL` | 否 | CDN/自定义域名 | 留空使用 COS 默认域名；配置时不要以 `/` 结尾。 |
| `COS_PATH_PREFIX` | 否 | `learning-planet` | 对象键统一前缀，便于权限和生命周期管理。 |

`NODE_ENV=production` 时登录 Cookie 带有 `Secure` 属性，正式环境必须通过 HTTPS 访问。

普通会话有效期为 12 小时。电脑浏览器登录时前端默认申请 30 天免登；手机浏览器仍使用普通会话。退出登录、账号禁用以及管理员重置家长或学生密码会使对应会话失效。账号本人修改密码后，当前会话仍保持登录。

## 4. 推荐部署：Docker Compose

### 4.1 上传代码

将以下文件完整上传到服务器同一目录：

```text
Dockerfile
docker-compose.yml
package.json
server.mjs
storage.mjs
scripts/
index.html
app.js
styles.css
assets/
.env
```

运行时业务逻辑只依赖 Node.js 24 内置模块，Lucide 图标已放在 `assets/vendor/` 中随应用发布。不要遗漏 `assets/category-icons/`、登录页图片和成长星球图片。

不要上传本地 `data/` 目录，除非正在执行数据迁移或恢复。

### 4.2 配置腾讯云 COS

1. 创建与服务器同地域或邻近地域的 COS 存储桶。
2. 为应用创建专用 CAM 子账号，只授权该存储桶 `learning-planet/*` 路径的对象上传和删除权限，不使用主账号永久密钥。
3. 确保对象可通过配置的 COS 域名或 CDN 域名读取，并为该域名启用 HTTPS。
4. 将 COS 环境变量写入服务器 `.env`，文件权限建议设为 `600`。
5. 如果使用 CDN/自定义域名，将其填入 `COS_PUBLIC_BASE_URL`；数据库会保存此公开 URL。

浏览器不会接触腾讯云密钥。上传内容先由 Node.js 校验，再由服务端签名写入 COS。

### 4.3 构建并启动

```bash
docker compose up -d --build
```

查看运行状态：

```bash
docker compose ps
docker compose logs --tail=100 learning-planet
```

本机检查：

```bash
curl -fsS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:4173/
```

预期首页返回 HTTP `200`。数据库保存在 Compose 逻辑名为 `learning-planet-data` 的命名卷中，并挂载到容器的 `/app/data`；实际卷名通常带 Compose 项目前缀。重建容器不会清空数据。

### 4.4 停止与启动

```bash
docker compose stop
docker compose start
```

不要使用 `docker compose down -v`，其中 `-v` 会删除数据库卷。普通 `docker compose down` 不会删除命名卷，但执行前仍应确认备份状态。

## 5. Nginx HTTPS 反向代理

以下示例假设域名为 `study.example.com`，应用监听服务器本机 `4173`：

```nginx
server {
    listen 80;
    server_name study.example.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name study.example.com;

    ssl_certificate     /etc/letsencrypt/live/study.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/study.example.com/privkey.pem;

    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:4173;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
    }
}
```

使用 `$remote_addr` 覆盖 `X-Forwarded-For`，避免客户端伪造来源 IP 绕过登录限流。

检查并重载配置：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

上线后访问：

```text
https://study.example.com/
```

### 5.1 前后端分开托管

如需由 Nginx 直接托管前端，可将 `index.html`、`app.js`、`styles.css` 和 `assets/` 发布到 `/var/www/learning-planet`，Node.js 后端继续监听 `127.0.0.1:4173`。前端和 API 仍应使用同一个域名：

```nginx
server {
    listen 443 ssl http2;
    server_name study.example.com;

    ssl_certificate     /etc/letsencrypt/live/study.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/study.example.com/privkey.pem;
    client_max_body_size 50m;

    root /var/www/learning-planet;
    index index.html;

    location /api/ {
        proxy_pass http://127.0.0.1:4173;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

不要将前端部署到与 API 不同的域名。当前前端使用同源 `/api/*` 地址，会话 Cookie 也按同源方式设计。

## 6. 直接使用 Node.js 部署

不使用 Docker 时，服务器必须安装 Node.js 24 或更高版本。

```bash
cd /opt/learning-planet
NODE_ENV=production HOST=127.0.0.1 PORT=4173 APP_TIMEZONE=Asia/Shanghai npm start
```

必须从项目根目录启动，因为静态资源路径和 `data/` 数据目录均相对于当前工作目录解析。

首次启动会自动创建 `data/`、数据库表、索引、内置任务分类和超级管理员；后续启动会在原数据库上执行向前兼容迁移，不会生成测试家长、测试学生或模拟任务。

建议使用 systemd 托管进程：

```ini
[Unit]
Description=Learning Planet
After=network.target

[Service]
Type=simple
User=learning-planet
Group=learning-planet
WorkingDirectory=/opt/learning-planet
Environment=NODE_ENV=production
Environment=HOST=127.0.0.1
Environment=PORT=4173
Environment=APP_TIMEZONE=Asia/Shanghai
ExecStart=/usr/bin/node /opt/learning-planet/server.mjs
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
```

启用服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now learning-planet
sudo systemctl status learning-planet
```

`learning-planet` 系统用户必须对 `/opt/learning-planet/data` 具有读写权限，对程序文件只需读取权限。

## 7. 数据备份

### 7.1 Docker 停机备份

SQLite 开启 WAL 模式。最稳妥的简单备份方式是在短暂停机期间复制整个 `data/` 目录：

```bash
CONTAINER_ID=$(docker compose ps -q learning-planet)
docker compose stop learning-planet
mkdir -p backup
docker cp "$CONTAINER_ID:/app/data" "./backup/data-$(date +%Y%m%d-%H%M%S)"
docker compose start learning-planet
```

备份应至少每日执行一次，并同步到另一台服务器或对象存储。不要只复制 `.db` 而遗漏同一时刻的 `-wal` 和 `-shm` 文件。COS 中的对象不在 SQLite 备份内，还应为存储桶启用版本控制或定期复制到备份桶。

### 7.2 Node.js 停机备份

```bash
sudo systemctl stop learning-planet
cp -a /opt/learning-planet/data "/backup/learning-planet-data-$(date +%Y%m%d-%H%M%S)"
sudo systemctl start learning-planet
```

### 7.3 备份保留建议

- 每日备份保留 14 天。
- 每周备份保留 8 周。
- 每月备份保留 12 个月。
- 每月至少进行一次恢复演练。

## 8. 数据恢复

恢复前确认备份目录中包含 `learning-planet.db`，并停止应用写入。

Docker 部署：

```bash
CONTAINER_ID=$(docker compose ps -q learning-planet)
docker compose stop learning-planet
docker cp ./backup/data-YYYYMMDD-HHMMSS/. "$CONTAINER_ID:/app/data/"
docker compose start learning-planet
```

Node.js 部署：

```bash
sudo systemctl stop learning-planet
cp -a /backup/learning-planet-data-YYYYMMDD-HHMMSS/. /opt/learning-planet/data/
sudo chown -R learning-planet:learning-planet /opt/learning-planet/data
sudo systemctl start learning-planet
```

恢复后检查登录、学生列表、任务列表、审核中心和统计页面。

## 9. 版本升级与回滚

### 9.1 升级前

1. 备份数据库。
2. 记录当前代码版本或镜像标签。
3. 在隔离测试数据库上运行账户、任务工作流和任务模板接口回归测试。
4. 确认新版本支持现有 SQLite 结构。

发布前至少执行：

```bash
npm test
node --check server.mjs
node --check app.js
```

项目的 `npm test` 会启动临时服务、使用系统临时目录下的新数据库，并在测试结束后自动删除该目录。回归测试不得连接或覆盖正式环境的 `data/learning-planet.db`。

### 9.2 Docker 升级

```bash
docker compose build --no-cache learning-planet
docker compose up -d learning-planet
docker compose logs --tail=100 learning-planet
```

服务启动时会自动执行向前兼容的数据库字段和新表迁移，包括任务资源、任务模板及相关索引。升级过程中不得同时运行两个指向同一 SQLite 卷的不同版本实例。

### 9.3 从数据库迁移历史上传文件

首次升级到 COS 存储版本时，先配置 COS 环境变量并构建新镜像，然后停止应用并执行一次迁移：

```bash
docker compose stop learning-planet
docker compose run --rm learning-planet npm run migrate:storage
docker compose up -d learning-planet
```

直接使用 Node.js 部署时：

```bash
sudo systemctl stop learning-planet
cd /opt/learning-planet
NODE_ENV=production npm run migrate:storage
sudo systemctl start learning-planet
```

迁移工具会先生成 `learning-planet.db.before-media-migration-*` 备份，再逐条上传头像、任务/模板资料和学生反馈。每条数据只有在上传成功后才写入 URL 并清空原 Base64，执行中断后可直接重跑。迁移完成后抽查头像、附件预览和下载，再按备份策略保存或清理迁移前备份。

### 9.4 回滚

1. 停止当前应用。
2. 恢复升级前代码或镜像。
3. 如升级包含数据库迁移，恢复升级前数据库备份。
4. 启动旧版本并完成业务检查。

## 10. 日常运维检查

建议监控以下项目：

- HTTPS 首页是否返回 `200`。
- Node.js 进程或容器是否持续运行。
- 日志中是否出现未捕获异常或连续 `500`。
- 数据卷剩余空间是否高于 20%。
- 数据库备份是否按计划生成并可读取。
- 登录失败次数是否异常增长。
- 上传资料增长速度是否异常。
- COS 请求错误率、流量、对象数量和 CDN 缓存命中率是否异常。

Docker 日志：

```bash
docker compose logs -f --tail=200 learning-planet
```

systemd 日志：

```bash
journalctl -u learning-planet -f -n 200
```

## 11. 安全注意事项

- 必须使用 HTTPS，禁止生产环境使用明文 HTTP 登录。
- 仅反向代理对公网开放，应用端口限制为本机或内网访问。
- 严格限制 `.env`、`data/` 和备份目录的文件权限。
- 不要将数据库、备份文件或用户上传内容提交到 Git。
- 超级管理员账号仅供平台管理使用，不应作为普通家长账号使用。
- 定期更新服务器、Docker、Node.js 24 LTS 安全版本和 Nginx。
- 当前验证码和登录失败计数保存在单进程内存中，因此生产环境应运行单个应用实例。
- SQLite 数据库也要求本系统保持单写入实例；不要让两个容器或两个 Node.js 进程同时挂载同一个 `data/` 目录。
- 如需多实例、高并发或跨主机部署，应先将 SQLite、验证码状态和登录限流迁移到共享数据库与 Redis。

## 12. 常见故障排查

### 页面能打开但无法登录

- 确认不是通过 `file://` 打开的 `index.html`。
- 确认浏览器访问的是 HTTPS 域名。
- 检查 `/api/auth/captcha` 是否被反向代理到 Node.js。
- 确认 `NODE_ENV=production` 时没有通过普通 HTTP 访问。

### 上传文件失败或图片无法显示

- 检查 `.env` 中 `COS_BUCKET` 是否包含 APPID，`COS_REGION` 是否与存储桶地域一致。
- 检查 CAM 子账号是否拥有目标路径的对象上传和删除权限。
- 检查 COS 默认域名或 `COS_PUBLIC_BASE_URL` 是否能通过 HTTPS 读取对象。
- 任务及任务模板资料最多 5 个文件，单文件最大 6 MB；学生图片或视频反馈最大 4 MB，头像最大 200 KB。

### 重启后数据丢失

- Docker 部署检查 `learning-planet-data:/app/data` 卷是否仍存在。
- Node.js 部署检查启动时的 `WorkingDirectory` 是否改变。
- 检查运行用户是否有 `data/` 写权限。

### 上传失败

- Nginx 的 `client_max_body_size` 应不小于 `50m`。
- 头像原图前端限制为 2 MB，并压缩为 256 × 256 JPEG；服务端最终接收数据最大 200 KB。
- 学生反馈单文件最大 4 MB，支持 JPG、PNG、WebP、MP4 和 WebM。
- 任务及任务模板资料最多 5 个文件，单文件最大 6 MB，支持图片、MP4/WebM、PDF、Office、TXT 和 ZIP。
- 文件使用 Base64 随 JSON 请求上传，传输体积会比原文件更大，因此相关应用接口允许约 45 MB 请求体，反向代理建议保持 `50m` 或更高。

### 登录频繁提示稍后再试

单个来源 IP 在 15 分钟内最多允许 8 次失败登录。检查反向代理是否正确覆盖 `X-Forwarded-For`，避免所有用户被识别为同一来源或客户端伪造 IP。

## 13. 上线验收清单

- [ ] 使用 HTTPS 域名打开登录页。
- [ ] 学生登录与家长登录验证码有效。
- [ ] 超级管理员只能通过家长登录入口进入。
- [ ] 新增家长、学生及多家长关联正常。
- [ ] 普通家长只能看到关联学生。
- [ ] 普通家长看不到任务分类菜单，分类新增、修改、删除接口返回 `403`。
- [ ] 创建私有/开放任务模板、模板所有权限制和批量分配正常。
- [ ] 单日或连续日期任务分配正常，连续范围不超过 30 天。
- [ ] 学生和家长可以只读查看任务详情，查看详情不会改变任务状态。
- [ ] 分配任务、修改未审核任务、附件查阅、保存草稿和提交正常。
- [ ] 待审核数量、审核结果和奖励星星一致。
- [ ] 学生切换后首页统计同步变化。
- [ ] 周榜、月榜和自定义统计正确。
- [ ] 无任务时完成率显示“--”或“没有任务”，不计为 100%。
- [ ] 电脑浏览器 30 天免登、手机普通会话和退出登录均符合预期。
- [ ] 手机端家长菜单、任务列表和成长奖励空状态无横向溢出。
- [ ] 退出登录确认弹窗和会话失效正常。
- [ ] 数据库备份、恢复和磁盘告警已配置。
- [ ] 新上传头像、任务资料和学生反馈在数据库中只保存 URL，COS 对象可预览和下载。
- [ ] 历史 Base64 数据迁移完成，迁移前数据库备份可用。
